# Keyword clusters + build queue — <SITE NAME>

The ranked queue the rail builds from. The dashboard brain-feeder refreshes this
monthly from live GSC + SEMrush; a human merges the refresh. Starts empty.

## GSC baseline
<pulled by the brain-feeder: ranking queries, positions, impressions>

## Clusters (hub -> spokes)
<primary hub terms and their supporting keywords>

## Build queue (ranked)
Only rows here that ALSO have a brief in seo/build-briefs/ are eligible to build.

- **P1 (build now):** <slug — why (GSC query + position)>
- **P2 (next):** <slug — why>
- **P3 (backlog):** <slug — why>
